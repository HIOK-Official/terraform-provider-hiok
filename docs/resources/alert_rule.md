---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "hiok_alert_rule Resource - hiok"
subcategory: ""
description: |-
  Emails when a measurement crosses a line, e.g. availability below 100 for 2 minutes (the resource is down), or CPU above 90.
---

# hiok_alert_rule (Resource)

Emails when a measurement crosses a line, e.g. availability below 100 for 2 minutes (the resource is down), or CPU above 90.

## Example Usage

```terraform
# Email when the VM is down for 2 minutes.
resource "hiok_alert_rule" "vm_down" {
  resource_type  = "virtualmachine"
  resource_id    = hiok_virtual_machine.web.id
  name           = "web-down"
  metric         = "availability"
  comparison     = "lt"
  threshold      = 100
  window_minutes = 2
  severity       = "critical"
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `metric` (String) availability, cpu, memoryPercent or diskPercent.
- `name` (String)
- `resource_id` (String) The resource's id.
- `resource_type` (String) virtualmachine, container, containerinstance, containerapp, …
- `threshold` (Number)

### Optional

- `comparison` (String) gt, gte, lt or lte.
- `description` (String)
- `enabled` (Boolean)
- `notify_email` (String) Default: the owner.
- `severity` (String) info, warning or critical.
- `window_minutes` (Number) Minutes the condition must hold.

### Read-Only

- `id` (String) The ID of this resource.
- `state` (String) ok or firing.
